import React from "react";

const ProgressBar = ({
  section,
  currentSection,
}: {
  section: string;
  currentSection: number;
}) => {
  return <div>ProgressBar</div>;
};

export default ProgressBar;
